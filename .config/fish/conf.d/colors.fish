# Non-default colors (rest of the scheme matches fish's built-in default theme).
# Kept here rather than in universal fish_variables so it's tracked and machine-independent.
set -g fish_color_command blue
set -g fish_color_search_match white --background=brblack
